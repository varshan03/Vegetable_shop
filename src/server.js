// src/config.js
const config = {
      baseURL: "http://localhost:4000" // same as BASE_URL in backend
  // baseURL: "https://vegetable-shop-back.onrender.com" // same as BASE_URL in backend
};

export default config;
